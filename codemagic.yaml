workflows:
  ios-build:
    name: Build iOS app
    max_build_duration: 60
    environment:
      xcode: latest
    scripts:
      - name: Build app
        script: xcodebuild -workspace Reinos-de-Aegarth.xcworkspace -scheme Reinos-de-Aegarth -configuration Release clean build
    artifacts:
      - build/Release-iphoneos/*.ipa
